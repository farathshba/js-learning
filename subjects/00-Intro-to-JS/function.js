/**
 * Create getAverage() function
 * with a and b param
 */
function getAverage(a,b){
	var age = (a + b) / 2;
	// console.log(age);
	return age;
};

var myResult = getAverage(6,7);
console.log("You are " + myResult);
